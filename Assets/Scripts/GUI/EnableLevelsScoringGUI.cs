using UnityEngine;
using System.Collections;

public class EnableLevelsScoringGUI : MonoBehaviour {

   // public GameObject levelsMenu;
    //public GameObject scoring;
    //public GameObject calib;
    

	// Use this for initialization
	void Start () 
    {
        //levelsMenu.SetActive(false);
        //scoring.SetActive(false);
        //calib = GameObject.FindGameObjectWithTag("Cal");
	}
	
	// Update is called once per frame
	void Update () 
    {/*
        if (Input.GetKeyDown(KeyCode.L) && !levelsMenu.activeSelf)
        {
            levelsMenu.SetActive(true);
        }
        else if (Input.GetKeyDown(KeyCode.L) && levelsMenu.activeSelf)
        {
            levelsMenu.SetActive(false);
        }
        /*
        if (Input.GetKeyDown(KeyCode.P) && !scoring.activeSelf)
        {
            scoring.SetActive(true);
        }

        else if (Input.GetKeyDown(KeyCode.P) && scoring.activeSelf)
        {
            scoring.SetActive(false);
        }
        /*
        if (Input.GetKeyDown(KeyCode.W) && !calib.activeSelf)
        {
            calib.SetActive(true);
        }

        else if (Input.GetKeyDown(KeyCode.W) && calib.activeSelf)
        {
            calib.SetActive(false);
        }*/
	}
}
